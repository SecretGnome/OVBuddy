# OVBuddy Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# SD Card Setup Configuration
# These are used by setup-sd-card.sh to configure the Raspberry Pi
# ============================================================================

# WiFi Configuration
WIFI_SSID="YourWiFiNetwork"
WIFI_PASSWORD="YourWiFiPassword"
WIFI_COUNTRY="US"

# Raspberry Pi Configuration
HOSTNAME="ovbuddy"
USERNAME="pi"
USER_PASSWORD="YourSecurePassword"

# ============================================================================
# Deployment Configuration
# These are used by deploy.sh and other scripts to connect to the Pi
# ============================================================================

# Pi Connection (will be auto-constructed from HOSTNAME and USERNAME if not set)
# Format: hostname.local or IP address
PI_HOST="ovbuddy.local"
PI_USER="pi"
PI_PASSWORD="YourSecurePassword"

# Note: PI_HOST will be automatically set to ${HOSTNAME}.local if not specified
# Note: PI_USER and PI_PASSWORD will use USERNAME and USER_PASSWORD if not specified

